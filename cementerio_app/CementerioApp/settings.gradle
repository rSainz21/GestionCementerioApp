pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // JitPack — para MPAndroidChart (gráficas)
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "CementerioApp"
include ':app'
