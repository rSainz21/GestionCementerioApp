server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Expo Router web export => SPA: todo ruta debe volver a index.html
  location / {
    try_files $uri $uri/ /index.html;
  }
}

